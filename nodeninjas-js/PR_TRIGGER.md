Prueba para forzar diff en PR

Este archivo se añadió sólo para forzar un commit en la rama `interfaz-3-gestion-ofertas` y que GitHub muestre diferencias respecto a `develop`. El contenido no afecta la aplicación.